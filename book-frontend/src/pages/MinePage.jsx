import { useState } from 'react'
import { useAuth } from '../auth/AuthContext'
export default function MinePage({onOpenOrders,onOpenBookshelf,onOpenFavorites,onOpenReviews,onOpenHistory,onOpenAddresses}){const auth=useAuth();const[mode,setMode]=useState('login');const[form,setForm]=useState({username:'',phone:'',password:'',confirmPassword:''});const[msg,setMsg]=useState('');const[editing,setEditing]=useState(false);const[changingPassword,setChangingPassword]=useState(false);const[profile,setProfile]=useState({});const[passwordForm,setPasswordForm]=useState({old_password:'',new_password:'',confirm_password:''});const[passwordErrors,setPasswordErrors]=useState({old_password:'',confirm_password:''});const[busy,setBusy]=useState(false);if(auth.authLoading)return <div className="page-card">正在恢复登录状态…</div>;if(!auth.user){const submit=async e=>{e.preventDefault();setMsg('');setBusy(true);try{if(mode==='register'){if(form.password!==form.confirmPassword)throw new Error('两次密码不一致');await auth.register(form);setMode('login');setForm({username:form.username,phone:'',password:'',confirmPassword:''});setMsg('注册成功，请登录')}else{await auth.login(form);setForm(x=>({...x,password:''}))}}catch(e){setMsg(e.message)}finally{setBusy(false)}};return <div className="auth-card"><div className="auth-tabs"><button onClick={()=>setMode('login')}>登录</button><button onClick={()=>setMode('register')}>注册</button></div><form className="auth-form" onSubmit={submit}><label>用户名<input value={form.username} onChange={e=>setForm({...form,username:e.target.value})} required/></label>{mode==='register'&&<label>手机号<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} required/></label>}<label>密码<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label>{mode==='register'&&<label>确认密码<input type="password" value={form.confirmPassword} onChange={e=>setForm({...form,confirmPassword:e.target.value})} required/></label>}<button className="primary-btn" disabled={busy}>{busy?'处理中…':mode==='login'?'登录':'注册'}</button></form>{msg&&<div className="error-msg">{msg}</div>}</div>}const u=auth.user;const start=()=>{setChangingPassword(false);setProfile({username:u.username||'',avatar:u.avatar||'',gender:u.gender||'',email:u.email||''});setEditing(true)};const startChangePassword=()=>{setEditing(false);setMsg('');setPasswordForm({old_password:'',new_password:'',confirm_password:''});setPasswordErrors({old_password:'',confirm_password:''});setChangingPassword(true)};const save=async e=>{e.preventDefault();setBusy(true);try{await auth.updateProfile(profile);setEditing(false);setMsg('个人信息已更新')}catch(err){setMsg(err.message)}finally{setBusy(false)}};const submitChangePassword=async e=>{e.preventDefault();setMsg('');setPasswordErrors({old_password:'',confirm_password:''});if(passwordForm.new_password!==passwordForm.confirm_password){setPasswordErrors({old_password:'',confirm_password:'新密码与确认密码不一致'});return}setBusy(true);try{await auth.changePassword(passwordForm);setPasswordForm({old_password:'',new_password:'',confirm_password:''});setChangingPassword(false);setMsg('密码修改成功')}catch(err){if(err.message?.includes('原密码'))setPasswordErrors({old_password:err.message,confirm_password:''});else setMsg(err.message)}finally{setBusy(false)}};const cancelChangePassword=()=>{setPasswordForm({old_password:'',new_password:'',confirm_password:''});setPasswordErrors({old_password:'',confirm_password:''});setChangingPassword(false);setMsg('')};return <div className="mine-page"><div className="mine-header"><div className="avatar">{u.avatar?<img src={u.avatar} alt="头像"/>:u.username?.slice(0,1)}</div><div><div className="nickname">{u.username}</div><div>手机：{u.phone}</div></div></div>{changingPassword?<form className="profile-form" onSubmit={submitChangePassword}><label>原密码<input type="password" value={passwordForm.old_password} onChange={e=>{setPasswordForm({...passwordForm,old_password:e.target.value});setPasswordErrors({...passwordErrors,old_password:''})}} required/>{passwordErrors.old_password&&<span className="password-field-error">{passwordErrors.old_password}</span>}</label><label>新密码<input type="password" value={passwordForm.new_password} onChange={e=>setPasswordForm({...passwordForm,new_password:e.target.value})} minLength="6" required/></label><label>确认新密码<input type="password" value={passwordForm.confirm_password} onChange={e=>{setPasswordForm({...passwordForm,confirm_password:e.target.value});setPasswordErrors({...passwordErrors,confirm_password:''})}} minLength="6" required/>{passwordErrors.confirm_password&&<span className="password-field-error">{passwordErrors.confirm_password}</span>}</label><button className="primary-btn" disabled={busy}>{busy?'处理中…':'确认修改'}</button><button type="button" className="logout-btn" onClick={cancelChangePassword} disabled={busy}>取消</button></form>:editing?<form className="profile-form" onSubmit={save}><label>用户名<input value={profile.username} onChange={e=>setProfile({...profile,username:e.target.value})}/></label><label>头像<input value={profile.avatar} onChange={e=>setProfile({...profile,avatar:e.target.value})}/></label><label>邮箱<input type="email" value={profile.email} onChange={e=>setProfile({...profile,email:e.target.value})}/></label><label>性别<select value={profile.gender} onChange={e=>setProfile({...profile,gender:e.target.value})}><option value="">未设置</option><option value="M">M</option><option value="F">F</option></select></label><button className="primary-btn" disabled={busy}>保存</button></form>:<div className="profile-summary"><p>邮箱：{u.email||'未设置'}</p><p>性别：{u.gender||'未设置'}</p><button className="primary-btn" onClick={start}>编辑个人信息</button><button className="primary-btn" onClick={startChangePassword}>修改密码</button></div>}<div className="mine-actions"><button onClick={onOpenOrders}>我的订单</button><button onClick={onOpenBookshelf}>我的书架</button><button onClick={onOpenFavorites}>我的收藏</button><button onClick={onOpenReviews}>我的评价</button><button onClick={onOpenHistory}>浏览历史</button><button onClick={onOpenAddresses}>收货地址管理</button></div>{msg&&<div className="mine-hint">{msg}</div>}<button className="logout-btn" onClick={auth.logout}>退出登录</button></div>}

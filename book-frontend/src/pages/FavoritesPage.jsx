import { useEffect,useState } from 'react'
import { getFavorites,toggleFavorite } from '../api/interactions'
function Card({item,onSelect,onRemove,busy}){const b=item.book;return <article className="interaction-card"><div className="interaction-cover">{b.coverImage?<img src={b.coverImage} alt={b.title}/>:b.title?.slice(0,1)}</div><div><h3>{b.title||'图书'}</h3><p>{b.author}</p><p>{b.price} · {item.bookIsOnSale===false?'已下架':'在售'}</p><small>{item.createdAt||''}</small><div><button className="secondary-btn" disabled={item.bookIsOnSale===false} onClick={()=>onSelect(b)}>查看详情</button><button className="text-btn" disabled={busy} onClick={()=>onRemove(b.id)}>取消收藏</button></div></div></article>}
export default function FavoritesPage({onBack,onSelectBook}){const[items,setItems]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[busy,setBusy]=useState(null);useEffect(()=>{let live=true;getFavorites().then(x=>live&&setItems(x)).catch(e=>live&&setError(e.message)).finally(()=>live&&setLoading(false));return()=>{live=false}},[]);const remove=async id=>{setBusy(id);try{const r=await toggleFavorite(id);if(!r.isFavorite)setItems(x=>x.filter(i=>i.bookId!==id))}catch(e){setError(e.message)}finally{setBusy(null)}};if(loading)return <div className="page-card">加载中…</div>;return <div className="interaction-page"><button className="back-btn" onClick={onBack}>返回</button><h2>我的收藏</h2>{error&&<div className="error-msg">{error}<button onClick={()=>location.reload()}>重试</button></div>}{!items.length&&!error&&<div className="empty-state">暂无收藏</div>}<div className="interaction-list">{items.map(i=><Card key={i.id} item={i} busy={busy===i.bookId} onSelect={onSelectBook} onRemove={remove}/>)}</div></div>}
